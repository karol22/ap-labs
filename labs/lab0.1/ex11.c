#include <stdio.h>
int main()
{
    printf("I would test the program with lines with multiple tabs and blank spaces, also, empty lines or single words.\n");
    printf("The inputs that may uncover bugs are those with several scape characters");
}